# syntax=docker/dockerfile:1.7
# PLACEHOLDER — see RAHMA_WASM_DEPLOYMENT_PLAN.md.
FROM alpine:3.20 AS placeholder
RUN echo "Rahma child-safety WASM runtime — placeholder Dockerfile" > /readme.txt
CMD ["cat", "/readme.txt"]

[package]
name = "fatwa-policy-gate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Rahma fatwa-policy gate (FOUNDATION — refuses every public fatwa answer until a real scholar approval is wired)"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

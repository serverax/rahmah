[package]
name = "citation-gate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Deterministic Rahma citation-requirement policy (Rust + WASM port of citation-requirement.js)"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

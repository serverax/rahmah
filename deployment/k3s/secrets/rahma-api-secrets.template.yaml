# =============================================================================
# Rahma — rahma-api-secrets TEMPLATE.
#
# This file is a TEMPLATE only. NEVER commit real values.
#
# Render locally with operator-supplied values, then apply directly:
#   cp rahma-api-secrets.template.yaml rahma-api-secrets.rendered.yaml
#   # edit, fill in real values, then:
#   kubectl --kubeconfig=<sakina-safe> apply -f rahma-api-secrets.rendered.yaml
#   # then delete the rendered file from disk:
#   rm rahma-api-secrets.rendered.yaml
#
# All placeholder values below contain only REPLACE_ME. The repo-level
# .gitignore already excludes *.rendered.yaml.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: rahma-api-secrets
  namespace: rahma-api
  labels:
    app.kubernetes.io/part-of: rahma
    app.kubernetes.io/managed-by: k3s
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_ME_postgres://rahma_app:REPLACE_ME_PASSWORD@rahma-postgres.rahma-data.svc.cluster.local:5432/rahma?sslmode=disable"
  REDIS_URL: "REPLACE_ME_redis://rahma-redis.rahma-data.svc.cluster.local:6379/0"
  JWT_SECRET: "REPLACE_ME_64_byte_random_token"
  SESSION_SECRET: "REPLACE_ME_64_byte_random_token"
  ADMIN_EMAIL: "REPLACE_ME_admin@example.org"
  ADMIN_PASSWORD_HASH: "REPLACE_ME_argon2id_hash"
  SHEIKH_HASAN_EMAIL: "REPLACE_ME_sheikh.hasan@example.org"
  SHEIKH_HASAN_PASSWORD_HASH: "REPLACE_ME_argon2id_hash"
  # Email-hash allow-lists (sha-256 of lowercased email). Comma-separated.
  SHEIKH_ALLOWED_EMAIL_HASHES: "REPLACE_ME_sha256_hex_comma_separated"
  ADMIN_ALLOWED_EMAIL_HASHES: "REPLACE_ME_sha256_hex_comma_separated"

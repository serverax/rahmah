apiVersion: v1
kind: Service
metadata:
  name: rahma-web
  namespace: rahma-web
  labels:
    app.kubernetes.io/name: rahma-web
    app.kubernetes.io/part-of: rahma
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: rahma-web

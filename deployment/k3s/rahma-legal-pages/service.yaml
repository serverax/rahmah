apiVersion: v1
kind: Service
metadata:
  name: rahma-legal-pages
  namespace: rahma-legal-pages
  labels:
    app.kubernetes.io/name: rahma-legal-pages
    app.kubernetes.io/part-of: rahma
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rahma-legal-pages
  ports:
    - name: http
      port: 80
      targetPort: http

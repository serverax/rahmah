# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
.idea/
.vscode/
*.iml
*.lock

# Android / iOS native directories — generated per workstation by
# `flutter create .`. NEVER commit signing keystores or provisioning profiles.
android/
ios/
*.jks
*.keystore
*.mobileprovision
*.cer
*.p8
*.p12

# Local env
.env
.env.*
